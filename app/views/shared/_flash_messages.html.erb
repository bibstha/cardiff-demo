<% flash.each do |type, message| %>
  <div class="mb-4 rounded-sm px-4 py-3 shadow-sm <%= type == 'notice' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700' %>" role="alert">
    <p class="text-sm"><%= message %></p>
  </div>
<% end %>
